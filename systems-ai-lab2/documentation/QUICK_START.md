# 🚀 Быстрый старт - Обратная цепочка рассуждений

### 1. Проверьте структуру файлов

```
📁 Ваш проект/
├── 📄 main.py                        # Главный файл
├── 📄 expert_system_backward.py      # Система с обратной цепочкой
├── 📄 colors.py                      # Цвета (из ЛР1)
├── 📄 rules.txt                      # База правил (из ЛР1)
└── 📄 README.md                      # Документация
```

### 2. Установите зависимости (если нужно)

```bash
pip install -r requirements.txt
```

### 3. Запустите программу

```bash
python main.py
```

### 4. Выберите пункт 1 "Запустить систему"

### 5. Введите цель для проверки

```
включить_основное_освещение=да
```

### 6. Следите за процессом доказательства! ✅

---

## 🎯 Три готовых примера для демонстрации

### 📋 Пример 1: Простой (для начала)

**Цель:** `включить_основное_освещение=да`

**Что происходит:**

- Система находит правило с этой целью
- Проверяет условия (время*суток=вечер, присутствие*людей=да)
- Оба условия уже известны
- ✅ Цель доказана за 1 шаг!

**Время:** ~10 секунд

---

### 📋 Пример 2: Средний (основная демонстрация)

**Цель:** `сигнал_тревоги=да`

**Что происходит:**

1. Система ищет правило для сигнала тревоги
2. Находит: нужны `движение_на_входе=да` И `включить_охрану=да`
3. Для `включить_охрану=да` ищет другое правило
4. Находит: нужны `присутствие_людей=нет` И `время_суток=день`
5. Проверяет эти условия - ОК
6. Доказывает `включить_охрану=да`
7. Спрашивает о `движение_на_входе` (нет в базе)
8. Пользователь отвечает "да"
9. ✅ Цель доказана через 2 уровня рекурсии!

**Время:** ~30 секунд

---

### 📋 Пример 3: Опровержение (показать разные случаи)

**Цель:** `выключить_все_освещение=да`

**Что происходит:**

- Система находит правило: ЕСЛИ присутствие*людей=нет ТО выключить*все_освещение=да
- Проверяет условие: присутствие_людей=да (есть люди!)
- ❌ Противоречие! Цель опровергнута

**Время:** ~10 секунд

---

## ❓ Типичные вопросы и ответы

### В: Чем отличается от прямой цепочки?

**О:** Прямая идет от фактов к выводам, применяя все подходящие правила. Обратная идет от цели к фактам, применяя только те правила, которые нужны для доказательства конкретной цели. Обратная эффективнее для проверки гипотез.

### В: Как работает рекурсия?

**О:** Когда нужно доказать цель, система проверяет, можно ли вывести её из правил. Каждое условие правила становится новой подцелью, и для неё рекурсивно вызывается тот же алгоритм.

### В: Что если цель недостижима?

**О:** Система попробует все применимые правила. Если ни одно не сработает, она либо найдет противоречие (опровержение), либо запросит информацию у пользователя, либо вернет "не доказано".

### В: Зачем нужна защита от бесконечной рекурсии?

**О:** Если в базе знаний есть циклические зависимости (A→B→C→A), система может зациклиться. Ограничение глубины рекурсии (50 уровней) предотвращает это.

### В: Можно ли доказать несколько целей сразу?

**О:** В текущей реализации - последовательно. Каждую цель нужно проверять отдельным запуском. Это типично для обратной цепочки.

---

## 🐛 Решение проблем

### Проблема: Кракозябры вместо русских букв

```bash
# Windows: в начале программы
import os
os.system("chcp 65001")

# Или запустите:
chcp 65001
python main.py
```

### Проблема: Не работают цвета

```python
# Установите colorama
pip install colorama

# Или в начале программы:
import colorama
colorama.init()
```

### Проблема: FileNotFoundError: rules.txt

```bash
# Убедитесь, что rules.txt в той же папке
ls rules.txt  # Linux/Mac
dir rules.txt  # Windows

# Или программа создаст его автоматически
```

### Проблема: RecursionError

```python
# Увеличьте лимит рекурсии:
self.max_depth = 100  # вместо 50

# Или проверьте правила на циклы
```

---